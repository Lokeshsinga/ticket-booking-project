import React,{useEffect,useState} from 'react';
import {Link} from 'react-router-dom';
import {api} from '../services/api.js';
export default function Events(){const [events,setEvents]=useState([]);useEffect(()=>{api.get('/events').then(r=>setEvents(r.data.events))},[]);return <main><section className="hero"><div className="hero-copy"><span className="eyebrow">Ticketly presents</span><h1>Make every night a premiere.</h1><p>Discover the films and live moments worth leaving home for. Choose a show, pick your perfect seat, and let the lights go down.</p><button onClick={()=>document.querySelector('#shows')?.scrollIntoView({behavior:'smooth'})}>Explore shows</button></div><div className="hero-art"><div className="orb"/></div></section><section id="shows"><div className="section-title"><div><span className="eyebrow">Curated for you</span><h2>Now showing</h2></div><span className="eyebrow">{events.length} experiences</span></div><div className="event-grid">{events.map(e=><article key={e._id}><span className="eyebrow">{e.type}</span><h2>{e.title}</h2><p>{e.description}</p><Link to={`/events/${e._id}`}>View showtimes →</Link></article>)}</div></section></main>}
