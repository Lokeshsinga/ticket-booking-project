import {useState}from'react';import{useNavigate}from'react-router-dom';import{api}from'../services/api.js';export default function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[error,setError]=useState(''),go=useNavigate();const submit=async x=>{x.preventDefault();try{const r=await api.post('/auth/login',{email:e,password:p});localStorage.setItem('token',r.data.token);go('/')}catch(x){setError(x.response?.data?.error)}};return <main><h1>Login</h1><form onSubmit={submit}><input placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input placeholder="Password" type="password" value={p} onChange={x=>setP(x.target.value)}/><button>Login</button><p>{error}</p></form></main>}
import React from 'react';
