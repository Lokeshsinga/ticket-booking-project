import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import auth from './routes/auth.js'; import shows from './routes/shows.js'; import bookings from './routes/bookings.js'; import waitlist from './routes/waitlist.js'; import catalog from './routes/catalog.js'; import { notFound,errors } from './middleware/errors.js'; import { env } from './config/env.js';
export const app=express(); app.use(helmet());app.use(cors({origin:env.clientUrl}));app.use(express.json({limit:'100kb'}));app.get('/health',(req,res)=>res.json({ok:true}));app.use('/api/auth',auth);app.use('/api/shows',shows);app.use('/api/bookings',bookings);app.use('/api/waitlist',waitlist);app.use('/api',catalog);app.use(notFound);app.use(errors);
